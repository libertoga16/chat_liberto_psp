ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.kotlinchat.ApplicationKt.module ]
    }
}

jwt {
    secret = "kotlinchat-secret-key-cambiar-en-produccion"
    issuer = "kotlinchat"
    audience = "kotlinchat-users"
    realm = "KotlinChat Server"
}
